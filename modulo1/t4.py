# Cómo descargar, instalar y configurar Python
        # Debido a que el navegador le dice al sitio web al que se ingresó, 
        # el sistema operativo que se utiliza, el único paso que se debe seguir 
        # es hacer clic en la versión de Python que se desea.

        # En este caso, selecciona Python 3. El sitio siempre te ofrece la última versión.

        # Si eres un usuario de Windows, utiliza el archivo .exe descargado y sigue todos los pasos.

        # Deja las configuraciones predeterminadas que el instalador sugiere por ahora, 
        # con una excepción: observa la casilla de verificación denominada Agregar Python 
        # 3.x a PATH y selecciónala.

        # Esto hará las cosas más fáciles.

        # Si eres un usuario de macOS, es posible que ya se haya preinstalado 
        # una versión de Python 2 en tu computadora, pero como estaremos trabajando con Python 3, aún deberás 
        # descargar e instalar el archivo .pkg correspondiente desde el sitio de Python.
        
        ##.....Primer programa  py --version